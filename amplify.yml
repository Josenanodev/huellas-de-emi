version: 1
frontend:
    phases:
        preBuild:
            commands:
                - 'npm ci --cache .npm --prefer-offline'
        build:
            commands:
                - echo "SECRET_MONGODB_URI=$SECRET_MONGODB_URI" >> .env.production
                - echo "SECRET_ADMIN_PASSWORD=$SECRET_ADMIN_PASSWORD" >> .env.production
                - 'npm run build'
                - 'mv node_modules ./.amplify-hosting/compute/default'
    artifacts:
        baseDirectory: .amplify-hosting
        files:
            - '**/*'
    cache:
        paths:
            - '.npm/**/*'
            